"use client";

// Re-export the existing Staff page component
// The component uses effectiveOrganizationId from auth context which is set by the layout
export { default } from "@/app/booking/staff/page";
